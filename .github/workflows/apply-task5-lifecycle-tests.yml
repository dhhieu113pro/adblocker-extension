name: Apply Task 5 lifecycle tests

on:
  push:
    branches:
      - feat/optional-site-access

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/optional-site-access
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply lifecycle E2E patch
        run: node scripts/task5-lifecycle-test-patch.mjs
      - name: Commit lifecycle E2E tests
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/e2e/extension.spec.mjs
          git diff --cached --quiet && exit 0
          git commit -m "test: verify optional site access lifecycle"
          git push origin HEAD:feat/optional-site-access
